{
  "name": "analysis-artifacts",
  "type": "module",
  "displayName": "Analysis-Artifacts",
  "description": "Save, organize, search, and export important analysis outputs from Jupyter notebooks in VS Code.",
  "version": "0.0.1",
  "publisher": "tt00",
  "license": "MIT",
  "icon": "resources/icon.png",
  "keywords": [
    "data-science",
    "notebook",
    "jupyter",
    "visualization",
    "clipboard",
    "productivity"
  ],
  "categories": [
    "Data Science",
    "Notebooks"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/datadeck/analysis-artifacts.git"
  },
  "homepage": "https://github.com/datadeck/analysis-artifacts#readme",
  "bugs": {
    "url": "https://github.com/datadeck/analysis-artifacts/issues"
  },
  "galleryBanner": {
    "color": "#1e1e1e",
    "theme": "dark"
  },
  "engines": {
    "vscode": "^1.80.0"
  },
  "activationEvents": [
    "onView:analysis-artifacts.sidebar",
    "onCommand:analysis-artifacts.clipOutput",
    "onCommand:analysis-artifacts.exportMarkdown",
    "onCommand:analysis-artifacts.clearDeck"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "viewsContainers": {
      "activitybar": [
        {
          "id": "analysis-artifacts",
          "title": "Analysis-Artifacts",
          "icon": "resources/icon.svg"
        }
      ]
    },
    "views": {
      "analysis-artifacts": [
        {
          "id": "analysis-artifacts.sidebar",
          "name": "Analysis-Artifacts",
          "type": "webview"
        }
      ]
    },
    "commands": [
      {
        "command": "analysis-artifacts.clipOutput",
        "title": "Add to Deck",
        "category": "Analysis-Artifacts",
        "icon": "resources/add-to-deck.svg"
      },
      {
        "command": "analysis-artifacts.exportMarkdown",
        "title": "Export to Markdown"
      },
      {
        "command": "analysis-artifacts.clearDeck",
        "title": "Clear All Clips"
      }
    ],
    "keybindings": [
      {
        "command": "analysis-artifacts.clipOutput",
        "key": "ctrl+shift+d",
        "mac": "cmd+shift+d"
      }
    ],
    "menus": {
      "notebook/cell/title": [
        {
          "command": "analysis-artifacts.clipOutput",
          "when": "notebookType == 'jupyter-notebook' || resourceExtname == '.ipynb'",
          "group": "inline@1"
        }
      ]
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run build",
    "clean": "node -e \"const fs=require('fs'); for (const path of ['out']) fs.rmSync(path, { recursive: true, force: true });\"",
    "compile": "tsc -p ./tsconfig.json",
    "watch": "tsc -watch -p ./tsconfig.json",
    "webview:dev": "cd webview && npm run dev",
    "webview:build": "cd webview && npm run build",
    "test": "jest",
    "build": "npm run clean && npm run compile && npm run webview:build",
    "package": "npx vsce package",
    "publish": "npx vsce publish"
  },
  "devDependencies": {
    "@types/jest": "^29.5.0",
    "@types/node": "^20.0.0",
    "@types/vscode": "^1.80.0",
    "jest": "^29.5.0",
    "ts-jest": "^29.1.0",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "uuid": "^14.0.0"
  }
}
